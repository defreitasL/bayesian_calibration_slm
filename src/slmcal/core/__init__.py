"""Calibration workflows.

Kept free of top-level imports so that ``import slmcal.core`` stays lightweight;
import the submodules directly, e.g.::

    from slmcal.core.workflow import CalibrationWorkflow
"""
